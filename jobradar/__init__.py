"""jobradar — personal job-vacancy radar (collect → filter → score → Telegram → web triage).

Entry point is `python -m jobradar` (see __main__.py). Layers: a core/domain
engine and a Flask + Jinja2 web layer on top of it.
"""
